## local.properties for /slack

``` properties
# The token to access Slack bot APIs.
slack.token=xxx
# The channel you want to post message to.
slack.channelId=xxx
```

Run `./gradlew assembleAndNotifyDebug` for testing.