rootProject.name = "composite-library"

pluginManagement {
    plugins {
        kotlin("jvm") version "1.6.10"
    }
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}